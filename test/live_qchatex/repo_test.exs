defmodule LiveQchatex.RepoTest do
  use LiveQchatex.DataCase

  # @TODO Complete this tests!
  # alias LiveQchatex.Models

  describe "Repo module main functions" do
    test "write/2 stores the given model" do
    end

    test "read/2 returns the matching model" do
    end

    test "delete/1 removes the given model" do
    end

    test "delete_all/2 removes the matching models" do
    end

    test "find/2 returns the matching models" do
    end

    test "count/1 returns the given model records count" do
    end
  end

  describe "Repo module helper functions" do
    test "cast/2 returns the given model with the given attrs applied" do
    end

    test "to_atom_map/1 returns the given map with all its keys as atoms" do
    end

    test "random_string/1 returns a random string with given length" do
    end
  end
end
