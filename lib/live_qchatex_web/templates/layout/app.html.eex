<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Live Qchatex · Elixir + Phoenix LiveView + PubSub + Presence + Mnesia + Memento + Cluster · Fiqus.coop</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
            <li><a href="<%= Routes.live_path(@conn, LiveQchatexWeb.LiveChat.Home) %>">Home</a></li>
            <li><a href="<%= repo_url() %>" target="_blank">Repo</a></li>
            <li><a href="<%= demo_url("/lqchatex-overview.pdf") %>" target="_blank">Overview PDF</a></li>
            <%= if !LiveQchatex.Application.env?(:prod) do %>
            <li><a href="<%= demo_url() %>" target="_blank">Live Demo</a></li>
            <% end %>
          </ul>
        </nav>
        <h1>Live Qchatex</h1>
        <a href="<%= Routes.live_path(@conn, LiveQchatexWeb.LiveChat.Home) %>" class="main-logo">
          <img src="<%= Routes.static_path(@conn, "/images/fiqus-logo.png") %>" alt="Fiqus Logo"/>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= raw get_flash(@conn, :info) %></p>
      <p class="alert alert-success" role="alert"><%= raw get_flash(@conn, :success) %></p>
      <p class="alert alert-danger" role="alert"><%= raw get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <footer><%= raw render_version() %></footer>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
